#include "main.h"

/**
 * _isdigit - checks for a digit (0 throught 9)
 * @c: An input alphabet to be checke
 * Return: 1 if c is a digit or  0 otherwise
 */

int _isdigit(int c)
{

	return (c >= 48 && c <= 57);
}
